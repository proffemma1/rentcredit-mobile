{
  "name": "rentcredit-mobile",
  "version": "1.0.0",
  "description": "RentCredit Mobile App",
  "scripts": {
    "build": "mkdir -p build && cp -r www/* build/",
    "copy": "npx cap copy",
    "sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/ios": "^5.0.0",
    "@capacitor/android": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
